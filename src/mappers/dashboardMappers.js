/**
 * Returns the initial dashboard stats structure.
 */
export function getInitialDashboardStats() {
  return {
    totalDocuments: 0,
    totalUsers: null,
    activeUsers: null,
    inactiveUsers: null,
  };
}
